import PropTypes from 'prop-types';
export default function Blog({blog}) {

    return (
        <div className="">
            
        </div>
    )
}

Blog.propTypes = {
    blog: PropTypes.object.isRequired
    }